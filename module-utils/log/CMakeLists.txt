set_property(SOURCE ${CMAKE_CURRENT_LIST_DIR}/segger/SEGGER_RTT_ASM_ARMv7M.S PROPERTY LANGUAGE C)
target_sources(${PROJECT_NAME}

		PRIVATE

		"${CMAKE_CURRENT_LIST_DIR}/segger/SEGGER_RTT.c"

		)

if((${PROJECT_TARGET} STREQUAL "TARGET_RT1051") AND (${SYSTEM_VIEW_ENABLED}))
target_sources(${PROJECT_NAME}

		PRIVATE

		"${CMAKE_CURRENT_LIST_DIR}/segger/SEGGER_RTT_ASM_ARMv7M.S"

		)
endif()
