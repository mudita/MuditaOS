name: CI

on: [pull_request]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Check Out
      uses: actions/checkout@v2
      with:
        token: ${{ secrets.GitHub_PAT }}
        submodules: recursive
        fetch-depth: 0
    - name: "Style checking"
      run: |
        ./config/pre-commit.hook --last
    - name: "Unit Tests"
      run: |
        ./configure.sh linux Debug && \
        pushd build-linux-Debug && \
        echo "JOBS=${JOBS:-8} | set to 8" &&\
        make -j 8 check && \
        popd 
    - name: "Build for RT1051"
      run: |
        ./configure.sh rt1051 Debug && \
        pushd build-rt1051-Debug && \
        echo "JOBS=${JOBS:-8} | set to 8" &&\
        make -j 8 && \
        popd && \
        uptime
