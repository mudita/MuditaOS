name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        token: ${{ secrets.GitHub_PAT }}
        submodules: true
    - uses: ./.github/actions/style-chek
      with:
        action: "style"
        #    - name: Style Check
        #      uses: docker://hub.docker.com/rwicik/pure_phone_build:latest
        #      run: |
        #        ./config/pre-commit-check-only.hook --last
        #        #- name: Checkout submodules
        #        #  uses: textbook/git-checkout-submodule-action@2.0.0
        #    - name: Configure - rt1051
        #      uses: docker://hub.docker.com/rwicik/pure_phone_build:latest
        #      run: |
        #        ./configure.sh rt1051 Release
        #    - name: Build - rt1051
        #      uses: docker://hub.docker.com/rwicik/pure_phone_build:latest
        #      run: |
        #        cd build-rt1051-Release && make -j
        #
