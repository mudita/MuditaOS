#pragma once

namespace harness
{

    // enum of all emulated events we can parse
    enum class Events
    {
        KeyPress = 0,

        Size /// last element for size purposes
    };

} // namespace harness
